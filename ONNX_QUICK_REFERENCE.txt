╔════════════════════════════════════════════════════════════════════════╗
║                 ONNX INSTALLATION - QUICK REFERENCE                    ║
║                    ERROR: Failed building wheel for onnx               ║
╚════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│ ⚡ QUICKEST FIX (Choose One)                                            │
└─────────────────────────────────────────────────────────────────────────┘

[1] Automated Fix (2 minutes):
    python fix_onnx.py

[2] Conda Fix (5 minutes) ⭐ MOST RELIABLE:
    fix_onnx_conda.bat
    
[3] Manual Fix (3 minutes):
    pip install --only-binary :all: onnx==1.16.1 onnxruntime==1.19.2

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔍 WHAT'S MY PYTHON VERSION?                                           │
└─────────────────────────────────────────────────────────────────────────┘

Run: python --version

┌──────────────┬────────────────────────────────────────────────────────┐
│ Python 3.13+ │ ⚠️  Use Conda or downgrade to 3.11                     │
│ Python 3.11  │ ✅ PERFECT - Use any fix above                         │
│ Python 3.10  │ ✅ PERFECT - Use any fix above                         │
│ Python 3.9   │ ⚠️  Use: onnx==1.15.0 onnxruntime==1.17.1             │
│ Python 3.8   │ ⚠️  Use: onnx==1.14.1 onnxruntime==1.16.3             │
│ Python ≤3.7  │ ❌ Too old - Upgrade to Python 3.10+                   │
└──────────────┴────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 📁 DOCUMENTATION FILES                                                  │
└─────────────────────────────────────────────────────────────────────────┘

ONNX_FIX_START_HERE.md          ← ⭐ Start here!
ONNX_SOLUTIONS_SUMMARY.md       ← All solutions overview
ONNX_WINDOWS_FIX_ULTIMATE.md    ← Complete Windows guide
TROUBLESHOOTING_ONNX.md         ← Detailed troubleshooting
ONNX_BUILD_ERROR_FIX.md         ← Understanding the error

┌─────────────────────────────────────────────────────────────────────────┐
│ 🛠️ AUTOMATED FIX SCRIPTS                                               │
└─────────────────────────────────────────────────────────────────────────┘

fix_onnx.py              - Python script, tries 8 versions (85% success)
fix_onnx_conda.bat       - Conda method for Windows (95% success) ⭐
fix_onnx_windows.bat     - Windows pip fix (70% success)
install_windows.bat      - Complete Windows install (80% success)

┌─────────────────────────────────────────────────────────────────────────┐
│ 💪 MANUAL COMMANDS BY PYTHON VERSION                                   │
└─────────────────────────────────────────────────────────────────────────┘

# Python 3.13
pip install --only-binary :all: onnx==1.16.1 onnxruntime==1.19.2
→ If fails, use Conda

# Python 3.11 / 3.10 (Best)
pip install --only-binary :all: onnx==1.16.1 onnxruntime==1.19.2
pip install --only-binary :all: onnx==1.17.0 onnxruntime==1.20.0

# Python 3.9
pip install --only-binary :all: onnx==1.15.0 onnxruntime==1.17.1

# Python 3.8
pip install --only-binary :all: onnx==1.14.1 onnxruntime==1.16.3

┌─────────────────────────────────────────────────────────────────────────┐
│ 🏆 RECOMMENDED METHOD (WINDOWS)                                         │
└─────────────────────────────────────────────────────────────────────────┘

1. Install Miniconda: https://docs.conda.io/en/latest/miniconda.html

2. Run in Anaconda Prompt:
   conda create -n trajectory python=3.11 -y
   conda activate trajectory
   conda install -c conda-forge onnx onnxruntime -y
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
   pip install -r requirements.txt

→ Works 95% of the time! ⭐

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔧 COMPLETE FRESH START                                                │
└─────────────────────────────────────────────────────────────────────────┘

pip uninstall -y onnx onnxruntime
pip cache purge
python -m pip install --upgrade pip setuptools wheel
pip install --only-binary :all: onnx==1.16.1 onnxruntime==1.19.2
python -c "import onnx, onnxruntime; print('✓ Success!')"

┌─────────────────────────────────────────────────────────────────────────┐
│ ✅ VERIFY INSTALLATION                                                 │
└─────────────────────────────────────────────────────────────────────────┘

# Test imports
python -c "import onnx, onnxruntime; print('✓ Works!')"

# Check versions
python -c "import onnx, onnxruntime; print(f'ONNX: {onnx.__version__}'); print(f'Runtime: {onnxruntime.__version__}')"

# Test functionality
python -c "from onnx import helper, TensorProto; print('✓ All good!')"

┌─────────────────────────────────────────────────────────────────────────┐
│ 🆘 IF NOTHING WORKS                                                    │
└─────────────────────────────────────────────────────────────────────────┘

Option A: Use WSL2 (Linux on Windows)
  wsl --install
  # Then use Linux commands

Option B: Use Docker
  docker run -it python:3.11 bash
  # Everything works inside container

Option C: Downgrade Python to 3.11
  # Download from python.org
  # Create new venv with 3.11

Option D: Read full troubleshooting
  TROUBLESHOOTING_ONNX.md

┌─────────────────────────────────────────────────────────────────────────┐
│ 📊 SUCCESS RATES                                                       │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────┬────────┬────────┬──────────────┐
│ Method                  │ Rate   │ Time   │ Difficulty   │
├─────────────────────────┼────────┼────────┼──────────────┤
│ Conda                   │ 95%    │ 5 min  │ Easy ⭐      │
│ fix_onnx.py             │ 85%    │ 2 min  │ Easy         │
│ WSL2                    │ 90%    │ 10 min │ Medium       │
│ Docker                  │ 99%    │ 15 min │ Medium       │
│ Manual pip              │ 70%    │ 5 min  │ Easy         │
│ Build from source       │ 30%    │ 60 min │ Hard         │
└─────────────────────────┴────────┴────────┴──────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ 💡 KEY CONCEPTS                                                        │
└─────────────────────────────────────────────────────────────────────────┘

--only-binary :all:   Force using pre-built wheels only
--prefer-binary       Prefer wheels, but allow building
--no-cache-dir        Don't use cached packages

Why it fails: ONNX needs C++ compiler, CMake, Protobuf to build from source
              Most Windows users don't have these installed

The fix: Force pip to use pre-built wheels with --only-binary flag
         Specify exact version to ensure wheel exists

┌─────────────────────────────────────────────────────────────────────────┐
│ 🎯 TL;DR - JUST TELL ME WHAT TO DO                                     │
└─────────────────────────────────────────────────────────────────────────┘

Windows:   fix_onnx_conda.bat  (installs Conda environment)
           OR
           python fix_onnx.py  (uses pip)

Linux:     pip install -r requirements.txt
           (usually just works)

Mac:       conda install -c conda-forge onnx onnxruntime
           (most reliable)

┌─────────────────────────────────────────────────────────────────────────┐
│ 📞 GETTING HELP                                                        │
└─────────────────────────────────────────────────────────────────────────┘

Run diagnostics:
  python --version
  pip --version
  python -c "import platform; print(platform.platform())"

Read docs:
  ONNX_FIX_START_HERE.md
  TROUBLESHOOTING_ONNX.md

Online:
  https://github.com/onnx/onnx/issues
  Stack Overflow (tag: onnx, python)

╔════════════════════════════════════════════════════════════════════════╗
║  Last Updated: December 2025                                           ║
║  Recommended: Use Conda for Windows (95% success rate)                 ║
║  Documentation: ONNX_FIX_START_HERE.md                                 ║
╚════════════════════════════════════════════════════════════════════════╝
